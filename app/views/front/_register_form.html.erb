<div class="input-group col-md-6">
  <%= form_tag(:register_form) do %>
      <div class="form-group">
        <%= label_tag :email, t('E-mail') %><br>
        <%= email_field :user, :email, {class: 'form-control'} %>
      </div>
      <div class="form-group">
        <%= label_tag :email, t('Password') %><br>
        <%= password_field :user, :new_password, {class: 'form-control'} %>
      </div>
      <div class="form-group">
        <%= label_tag :email, t('Password confirm') %><br>
        <%= password_field :user, :new_password_confirmation, {class: 'form-control'} %>
      </div>
      <div class="actions">
        <%= submit_tag(t('Register'), {class: 'btn btn-default'}) %>
      </div>
  <% end %>
</div>